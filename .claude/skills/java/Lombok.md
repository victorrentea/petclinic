## Lombok Usage

- `@Slf4j` for logging
- `@RequiredArgsConstructor` for constructor injection
- `@Builder` for value objects / records / DTOs needing fluent construction
- `@Getter` / `@Setter` selectively — prefer immutability where possible