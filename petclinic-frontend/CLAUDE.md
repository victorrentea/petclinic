# petclinic-frontend — Claude Instructions

Angular 16 SPA. Auto-loaded when working on any file under `petclinic-frontend/`.

## Code Preferences

- Disabled buttons and pagination links: always `cursor: default` (never `cursor: pointer` on disabled elements)
