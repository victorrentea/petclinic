# ast-grep — structural lint rules that enforce conventions written in AGENTS.md.
# Run manually:  sg scan -c .config/sgconfig.yml
# Test rules:    sg test -c .config/sgconfig.yml
#
# What it flags, concretely (rule no-lombok):
#     import lombok.Data;         <- error: Lombok was removed from this project
# demanded instead: accessors, constructors and LoggerFactory.getLogger() written out.
ruleDirs:
  - ../.ast-grep/rules
testConfigs:
  - testDir: ../.ast-grep/rule-tests
