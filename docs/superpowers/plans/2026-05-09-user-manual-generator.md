# User Manual Generator Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a project-scoped Claude slash command `/regen-manual` that crawls the running PetClinic frontend, captures screenshots, and writes/updates `docs/manual/manual.md` using the prior manual as a baseline checklist.

**Architecture:** Three deliverables — (1) `docs/manual/` scaffold, (2) `.claude/commands/regen-manual.md` slash command prompt, (3) the first end-to-end execution that produces the initial populated manual. No code, no tests, no CI changes.

**Tech Stack:** Markdown-only authoring. Slash command leverages `chrome-devtools-mcp` (already installed) for navigation and screenshot capture, and standard `Read`/`Glob`/`Grep`/`Bash`/`Write`/`Edit` tools for reasoning about the prior manual and the routing source files.

---

## Reference: live route inventory at time of plan

Discovered from `petclinic-frontend/src/app/**/*-routing.module.ts`. The slash command will rediscover this each run, but for plan-writing reference:

- `/welcome` — landing page
- `/owners`, `/owners/add`, `/owners/:id`, `/owners/:id/edit`, `/owners/:id/pets/add`
- `/pets`, `/pets/add`, `/pets/:id`, `/pets/:id/edit`, `/pets/:id/visits/add`
- `/visits`, `/visits/add`, `/visits/:id/edit`
- `/pettypes`, `/pettypes/add`, `/pettypes/:id/edit`
- `/vets`, `/vets/add`, `/vets/:id/edit`
- `/specialties`, `/specialties/:id/edit`

Feature modules wired in `app.module.ts`: Owners, Pets, Visits, PetTypes, Vets, Specialties, Parts.

---

## Task 1: Scaffold `docs/manual/`

**Files:**
- Create: `docs/manual/manual.md`
- Create: `docs/manual/screenshots/.gitkeep`

- [ ] **Step 1: Create the manual stub**

Write `docs/manual/manual.md` with exactly this content (newline at end):

```markdown
# PetClinic User Manual

_This file is regenerated by the `/regen-manual` slash command. Do not edit by hand — your changes will be overwritten on the next regeneration._

_Run `/regen-manual` after you have started the application stack with `./run-all.sh` to populate this file._
```

- [ ] **Step 2: Create the screenshots placeholder**

Write `docs/manual/screenshots/.gitkeep` with empty content. The directory must exist on first regeneration; the `.gitkeep` keeps git tracking it before any screenshot is added.

- [ ] **Step 3: Verify both files exist**

Run: `ls -la docs/manual/ docs/manual/screenshots/`
Expected: `manual.md` listed under `docs/manual/`; `.gitkeep` listed under `docs/manual/screenshots/`.

- [ ] **Step 4: Commit**

```bash
git add docs/manual/manual.md docs/manual/screenshots/.gitkeep
git commit -m "docs(manual): scaffold docs/manual for /regen-manual output"
```

---

## Task 2: Author the `/regen-manual` slash command

**Files:**
- Create: `.claude/commands/regen-manual.md`

- [ ] **Step 1: Write the slash command prompt**

Create `.claude/commands/regen-manual.md` with this exact content:

````markdown
---
description: Regenerate docs/manual/manual.md by crawling the running PetClinic UI in a browser. Diff-based — uses the prior manual as a checklist and only updates what changed.
---

# Regenerate the user manual

You are regenerating the end-user manual for the PetClinic application. Output: `docs/manual/manual.md` plus screenshots under `docs/manual/screenshots/`.

## Pre-flight (abort if either fails)

Run both checks in parallel:

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:4200/
curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/actuator/health
```

If either returns anything other than `200`, abort with this message and STOP:

> Cannot regenerate the manual: the application stack is not running. Start it with `./run-all.sh` and try again.

## Step 1 — Load the baseline

Read `docs/manual/manual.md`. Extract:

- **Documented sections** — every `## <Feature name>` heading and its subsection structure.
- **Screenshot inventory** — every `screenshots/<file>.png` path referenced.

If the file only contains the scaffolding stub (no `##` headings yet), this is a first-time run: the baseline is empty, every section will be "new".

## Step 2 — Discover the live route inventory

Read these files to enumerate routes:

- `petclinic-frontend/src/app/app-routing.module.ts`
- All `petclinic-frontend/src/app/**/*-routing.module.ts` files

Group routes by feature area (the folder name under `src/app/`, e.g. `owners`, `pets`, `vets`, `visits`, `pettypes`, `specialties`). Skip the `parts` folder — it holds the welcome page and 404, treat the welcome page as its own top section.

If the discovered route list looks suspiciously short (fewer than 5 features), fall back to crawling the rendered nav/sidebar in the browser at `http://localhost:4200/` and use the visible menu links instead.

## Step 3 — Reconcile baseline against current routes

For each feature area:

| Baseline has it? | Code has it? | Action |
|---|---|---|
| yes | yes | Visit, compare current screen vs prior screenshot. Unchanged → keep prose and screenshot verbatim. Changed → update prose, replace screenshot. |
| no | yes | New feature — write fresh section, capture screenshots. |
| yes | no | Removed feature — delete section, delete orphan screenshots from `docs/manual/screenshots/`. |

"Visually unchanged" means layout, labels, and visible controls match. Differences in seeded data (different owner names, different list ordering) do NOT count as changes.

## Step 4 — Capture and document each feature

For each feature area in route order, walk the happy path: list → detail → create → edit. Aim for 3–5 screenshots per feature, ~30–50 total.

Use `chrome-devtools-mcp` tools:

- `mcp__plugin_chrome-devtools-mcp_chrome-devtools__new_page` to open `http://localhost:4200/<route>`
- `mcp__plugin_chrome-devtools-mcp_chrome-devtools__resize_page` to set viewport to **1280×800**
- `mcp__plugin_chrome-devtools-mcp_chrome-devtools__navigate_page`, `click`, `fill`, `fill_form` to drive the UI
- `mcp__plugin_chrome-devtools-mcp_chrome-devtools__take_screenshot` with `fullPage: true` and `format: "png"` to capture

Save each screenshot to `docs/manual/screenshots/<feature>-<state>.png`. Naming: lowercase, hyphenated, descriptive of the state (`owners-list.png`, `owners-create.png`, `owners-detail.png`, `pets-add-to-owner.png`, `visits-add.png`, etc.).

If you create new owners/pets/visits while crawling for screenshots, that test data persists in the running database. That is acceptable — the manual is a screenshot artifact, not a state-restoring test.

## Step 5 — Write/update `manual.md`

Use this exact section template per feature:

```markdown
## <Feature name>

One paragraph describing what this area is for and what you can do here.

### Viewing the list
![](screenshots/<feature>-list.png)
Step-by-step prose for the read path.

### Creating a new <thing>
![](screenshots/<feature>-create.png)
Step-by-step prose for the write path.
```

Tone: instructional second-person ("Click *Add Owner* to register a new owner..."). Audience: clinic staff, not developers. Do NOT document REST endpoints, validation rules, or developer concerns.

Top of `manual.md` must contain:

1. `# PetClinic User Manual`
2. A one-paragraph intro: what the app is for, plus the "do not edit by hand — regenerated by `/regen-manual`" line.
3. A TOC linking to each `## <Feature name>` heading. Section order follows route order in `app-routing.module.ts` and the feature module imports in `app.module.ts`. Welcome section first.

## Step 6 — Clean up orphans

Delete any file in `docs/manual/screenshots/` that is not referenced from the regenerated `manual.md`. Use:

```bash
git status docs/manual/screenshots/
```

to confirm only intended additions/deletions are present.

## Step 7 — Report

Print one summary line in this format:

> Manual regenerated: N new sections (<list>), M updated (<list>), K removed (<list>), P screenshots replaced.

Then stop. Do NOT auto-commit — the user will review the diff and commit themselves.
````

- [ ] **Step 2: Verify the file is valid markdown and discoverable**

Run:

```bash
ls -la .claude/commands/regen-manual.md && head -5 .claude/commands/regen-manual.md
```

Expected: file exists, first line is `---`, second line is `description: Regenerate docs/manual/manual.md ...`.

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/regen-manual.md
git commit -m "feat(claude): add /regen-manual slash command for user manual"
```

---

## Task 3: First end-to-end run

This task executes the slash command for real to populate `docs/manual/manual.md` from the empty stub and produce the initial set of screenshots.

**Files:**
- Modify: `docs/manual/manual.md` (will be rewritten by the command)
- Create: many `docs/manual/screenshots/*.png` files (~30–50)

- [ ] **Step 1: Confirm the stack is up**

Run:

```bash
curl -s -o /dev/null -w "frontend=%{http_code} " http://localhost:4200/ && \
  curl -s -o /dev/null -w "backend=%{http_code}\n" http://localhost:8080/actuator/health
```

Expected: `frontend=200 backend=200`. If not, run `./run-all.sh` in another terminal first and re-check.

- [ ] **Step 2: Invoke the slash command**

Type `/regen-manual` in the Claude prompt. Wait for the command to complete. Expect several minutes — the crawl visits ~7 feature areas with 3–5 screenshots each.

- [ ] **Step 3: Verify the report line**

Confirm the command printed a "Manual regenerated:" summary line. For a first run, expect something like:

> Manual regenerated: 7 new sections (welcome, owners, pets, visits, pettypes, vets, specialties), 0 updated, 0 removed, ~30 screenshots replaced.

- [ ] **Step 4: Inspect the output**

Run:

```bash
ls docs/manual/screenshots/ | wc -l
wc -l docs/manual/manual.md
head -40 docs/manual/manual.md
```

Expected: 25–55 PNG files; manual.md is 100+ lines; head shows the title, intro paragraph, and TOC.

- [ ] **Step 5: Spot-check screenshots**

Open three of the generated PNGs (one list, one create form, one detail page) and confirm they render the actual UI at 1280px wide, not a blank or login screen.

- [ ] **Step 6: Commit the populated manual**

```bash
git add docs/manual/manual.md docs/manual/screenshots/
git commit -m "docs(manual): initial /regen-manual output"
```

---

## Self-review notes

**Spec coverage:**
- Output structure (single file, flat screenshots) → Tasks 1, 2 (template), 3 (verification).
- Diff-based regeneration → Task 2 Step 1 (Steps 1, 3 of the command itself).
- Pre-flight check → Task 2 Step 1 (pre-flight section of command); Task 3 Step 1 (verifies before invoking).
- chrome-devtools-mcp + 1280×800 + PNG full-page → Task 2 Step 1 (Step 4 of the command).
- Audience/tone/depth → Task 2 Step 1 (Step 5 of the command).
- Section template + TOC → Task 2 Step 1 (Step 5 of the command).
- Orphan cleanup → Task 2 Step 1 (Step 6 of the command).
- No source-code/test/CI changes → only `docs/manual/` and `.claude/commands/` touched.
- Initial empty stub committed alongside slash command → Task 1.

**Placeholder scan:** none (all steps have concrete commands or exact content).

**Type/name consistency:** screenshot path convention `screenshots/<feature>-<state>.png` is identical in spec template, command template, and Task 3 verification.
