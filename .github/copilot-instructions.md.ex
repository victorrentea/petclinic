# Deprecated — do not use

GitHub Copilot instructions now live in the repository root `AGENTS.md`,
which is loaded automatically. This is the former Copilot instructions file,
kept only as a marker. Do not add rules here — put them in `AGENTS.md`.
