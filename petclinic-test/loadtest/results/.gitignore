# Raw JMeter output is regenerated by every run and runs to tens of MB (11 MB for the
# 10k run alone). What is worth committing is the derived evidence — summary-*.txt and
# explain-*.txt — which is what anyone actually reads.
*.jtl
*.log
